spring:
  datasource:
    url: jdbc:mysql://localhost:3306/glimmerseed?useSSL=false&amp;serverTimezone=UTC
    username: root
    password: testPassword123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8080

glimmerseed:
  siliconflow:
    api-key: testApiKey456
    base-url: https://api.siliconflow.cn/v1

jwt:
  secret: your-secret-key-change-this-in-production
  expiration: 86400000

